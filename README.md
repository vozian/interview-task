The app was created using Nest.js framework and MongoDB database.

The Database URI is configured in .env

## Running the app with a database
```bash
$ docker-compose up
```

## Test

```bash
$ yarn test
```
